#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    int x, y;

    x = 12 / 3 * 4 + 5 * 15 + 24 % 4 / 2;
    y = ((12 / 3) * 4) + (5*15) + ((24%4)/2);
    cout << x << endl;
    cout << y << endl;

    return 0;
}
